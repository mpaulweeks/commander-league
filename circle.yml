## Customize the test machine
machine:

  # Version of ruby to use
  ruby:
    version:
      2.2.1


dependencies:
  pre:
    - gem install minitest
    - gem install nokogiri

test:
  override:
    - rake test
